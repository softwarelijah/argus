# Core
numpy>=1.24,<2.2
opencv-python>=4.8
pyyaml>=6.0
scipy>=1.10
lap>=0.4.0

# Detection / training
torch>=2.1
torchvision>=0.16
ultralytics>=8.1

# Export / acceleration (optional, install where supported)
onnx>=1.15
onnxruntime>=1.17
# tensorrt and pycuda are provided by the NVIDIA stack on the target
# device. They are intentionally not pinned here so the CPU install
# path stays clean. Install them on the Jetson / dGPU host:
#   pip install tensorrt pycuda

# Tooling
tqdm>=4.66
